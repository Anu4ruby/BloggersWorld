= form_for([@blog, @blog.comments.build]) do |f|
  .field
    = f.label :commenter
    %br/
    = f.text_field :name
  .field
    = f.label :body
    %br/
    = f.text_area :body
  .actions
    = f.submit  
